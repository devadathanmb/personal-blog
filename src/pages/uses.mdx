---
title: Uses
subtitle: The environment I've slowly assembled over the years.
description: Hardware, software, services, and tools Devadathan uses daily.
bgType: storm
ogImage: true
---

import BaseLayout from '~/layouts/BaseLayout.astro'
import StandardLayout from '~/layouts/StandardLayout.astro'
import Link from '~/components/base/Link.astro'
import UsesSection from '~/components/widgets/UsesSection.astro'
import LastUpdated from '~/components/widgets/LastUpdated.astro'
import usesData from '~/data/uses.json'

<BaseLayout
  title={frontmatter.title}
  description={frontmatter.description}
  bgType={frontmatter.bgType}
  ogImage={frontmatter.ogImage}
>
  <StandardLayout title={frontmatter.title} subtitle={frontmatter.subtitle}>
    <div slot="head" class="-mt-5 mb-6 ml-1 slide-enter animate-delay-200!">
      <LastUpdated date="2026-06-01" />
    </div>
    <Fragment slot="article">

This is the hardware, software, and setup I've come to rely on over years of work, curiosity, and tinkering.

Every tool here solved a problem at some point. The ones that remain are the ones I still trust when things matter.

{usesData.map((section) => (

  <UsesSection section={section} />
))}

---

{/* prettier-ignore */}
<div class="mt-8 mb-4 flex items-center gap-2 op-80 font-medium text-sm">
  <Link
    href="https://github.com/devadathanmb/mac-dots"
    external
    enableNewTabWarning
    class="inline-flex items-center gap-2 border-b-none!"
  >
    <div class="i-ph-github-logo-duotone"></div>
    <span>Check out my dotfiles repository</span>
  </Link>
</div>

<em>
  If you're curious about this concept, {/* prettier-ignore */}
  <Link href="https://uses.tech" external enableNewTabWarning>uses.tech</Link>{' '}
  has the full story.
</em>

    </Fragment>

  </StandardLayout>
</BaseLayout>
