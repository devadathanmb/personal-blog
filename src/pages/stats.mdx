---
title: Stats
subtitle: A few numbers behind the writing, listening, watching, and tinkering.
description: By the numbers — Devadathan's writing, music, watching, and code stats.
bgType: storm
ogImage: true
---

import PageLayout from '~/layouts/PageLayout.astro'
import StatsBoard from '~/components/widgets/StatsBoard.astro'

<PageLayout
  title={frontmatter.title}
  subtitle={frontmatter.subtitle}
  description={frontmatter.description}
  bgType={frontmatter.bgType}
  ogImage={frontmatter.ogImage}
>
  <Fragment slot="article">

None of this says much on its own, but I like looking back at it. The writing counts are exact. The rest are pulled live from the services I use, so they drift a little every day.

<StatsBoard />

  </Fragment>
</PageLayout>
